- name: copying {{ component }} service template
  ansible.builtin.template:
    src: "{{ component }}.service.j2"
    dest: /etc/systemd/system/{{ component }}.service



- name: daemon reload
  ansible.builtin.systemd_service:
    state: restarted
    daemon_reload: true
    name: "{{ component }}"