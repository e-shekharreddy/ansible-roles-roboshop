- name: call common role
  #ansible.builtin.import_role:  # it fail if i make any mistake so it is "STATIC'
  ansible.builtin.include_role: # it will excute directly so it is 'DYNAMIC'
    name: common
    tasks_from: include-vs-import.yaml
  when: os == "RedHat"
  loop: 
  - vadhan
  - sri 
