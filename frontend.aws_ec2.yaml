plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

# hostnames:
# - ip-address
# - public-ip-address  


# filters:
#   instance-state-name: running
#   tag:Name: frontend-dev

# compose:
#   ansible_host: private_ip_address


# keyed_groups:
# - separetor: ''
#   key: tags.Component  # we must use the tags while creating the servers then its easy to search