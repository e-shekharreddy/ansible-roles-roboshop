plugin: amazon.aws.aws_ec2
regions:
  - us-east-1   # Ansible > DYNAMIC INVENTORY concept go check timestamps 'my discussions'
                # ansible-playbook -i frontend.aws_ec2.yaml --list < this is command
# hostnames:
# - ip-address
# - private-ip-address  


# filters:
#   instance-state-name: running
#   tag:Name: frontend-dev

# compose:
#   ansible_host: private_ip_address


# keyed_groups:
# - separetor: ''
#   key: tags.Component  

# we must use the tags while creating the servers then its easy to search